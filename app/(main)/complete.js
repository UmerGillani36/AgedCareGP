import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AppointmentComplete = () => {
  return (
    <View>
      <Text>appointment-complete</Text>
    </View>
  );
};

export default AppointmentComplete;

const styles = StyleSheet.create({});
